<Page
    x:Class="CocktailPi.PumpsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CocktailPi"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <ListView x:Name="PumpList" ItemClick="listItemClicked" IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Pump">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1" Text="{x:Bind Number}" Margin="12,6,0,0"  Style="{ThemeResource BaseTextBlockStyle}" FontSize="18"/>
                        <!--<TextBlock Grid.Column="2" Text="{x:Bind Ingredient}" Margin="12,6,0,0"  Style="{ThemeResource BaseTextBlockStyle}" FontSize="18"/>-->
                        <ComboBox x:Name="cboIngredient" Text="{x:Bind Ingredient}" Margin="12,6,0,0"  Style="{ThemeResource BaseTextBlockStyle}" FontSize="18"/>
                        <Button x:Name="cmdPrime" Grid.Column="3" Content="Prime"/>
                        <Button x:Name="cmdRecover" Grid.Column="4" Content="Recover"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
